#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int compare(const void *a, const void *b) {
    return (*(int*)a - *(int*)b);
}

void asd_qsort(void *asd, int n, int size, int (*f)(const void *a, const void *b))
{





};

int main ()
{

    qsort();


    return 0;
}